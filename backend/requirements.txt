# Web framework
fastapi==0.121.0
uvicorn[standard]==0.53.0
python-multipart==0.0.32

# Config and validation
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0  # backs pydantic's EmailStr

# Database
sqlmodel==0.0.42
psycopg2-binary==2.9.13
alembic==1.20.0

# Auth
pyjwt==2.14.0
bcrypt==5.0.0  # used directly, no passlib: passlib is unmaintained and bcrypt's
               # own hashpw/checkpw covers hash+verify with no wrapper needed

# File extraction
pypdf==6.19.0
python-docx==1.2.0

# Scoring (embeddings)
fastembed==0.8.0

# LLM providers
groq==1.7.0
google-genai==2.23.0

# PDF export
weasyprint==70.0
jinja2==3.1.6

# Dev / test
pytest==8.4.2
pytest-asyncio==1.4.0
httpx==0.28.1
ruff==0.16.8
